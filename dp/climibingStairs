class Solution {
public:
    int climbStairs(int n) {
        vector<int> dp(n+1,-1);
        dp[0]=1; dp[1]=1;
        for (int i=2; i<=n; i++) {
            dp[i]=dp[i-2]+dp[i-1];//sum of ways to reach i-1 and i-2 is equal to ways to reach curr index
        }
        return dp[n];
    }
};